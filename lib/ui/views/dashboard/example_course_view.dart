import 'package:flutter/material.dart';

class ExampleCourseView extends StatelessWidget {
  const ExampleCourseView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}